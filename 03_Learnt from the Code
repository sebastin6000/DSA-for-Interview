1.if the question is asking boolean output so by default we set either "True" or "False" for its value and then convert later according to which boolean we want(T or F).
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
2.declare a variable inside a loop to get current value.
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
3.To extract indicidual digit from a 2 digit number, 3 digit till the n digit number and all these present inside a array
3.1.you have to convert every 2 digit, 3 digit into string 
3.2.now you can have the access for every indidual element in the string.
Input: nums = [131, 11, 48]
so the number 131 is convented into "131" so with this we can access individual characters.
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
